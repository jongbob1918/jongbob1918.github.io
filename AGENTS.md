# 저장소 작업 및 글쓰기 규칙

브랜치 운영은 저장소 전체에 적용한다. 블로그 글(`site/src/content/notes/`)과 프로젝트 상세 페이지(`project-data/`)에는 각각 해당하는 글쓰기 규칙만 적용한다.

## 브랜치 운영

- 이 저장소는 `main` 브랜치 하나만 사용한다.
- 작업 후 생성된 임시·기능 브랜치는 병합 또는 작업 종료 후 로컬과 원격에서 삭제한다.
- 변경사항은 `main`에 반영하고 `origin/main`으로 푸시한다.

## 블로그 글: 이해한 과정을 남긴다

- 블로그 글의 주된 목적은 작성자가 개념을 자기 말로 설명하며 이해를 확인하고, 나중에 다시 읽어도 사고 과정을 따라갈 수 있게 남기는 것이다. 완전한 교재처럼 모든 내용을 빠짐없이 설명하려 하지 않는다.
- 처음 쓸 때는 독자가 지금 아는 것에서 출발해, 다음에 생길 질문에 답하는 순서로 전개한다. 한 문단에서 한 질문이나 한 단계만 다루고, 새 개념은 앞의 예시·그림·계산에 연결한다. 고정된 문단 템플릿은 강제하지 않는다.
- 독자가 막힘없이 이해하는 데 필요한 최소한의 글자로 쓴다. 같은 뜻이라면 짧은 표현을 택하되, 정보를 빼서 되읽게 만들지는 않는다. 도입·요약·주의 문장을 반복하지 않고, 그림이나 계산에 이미 보이는 내용은 결론만 적는다.
- 쉬운 말로 설명하되 수학적 정확성을 지킨다. 부호, 단위, 계산, 용어가 틀렸다면 그 부분을 바로잡는다. 정확성을 위해 필요한 조건이 빠졌다면 짧게 보충한다.
- 초안을 검수할 때는 작성자의 문체와 설명 순서를 유지하되, 수정량을 최소화하는 데 집착하지 않는다. 가독성을 해치는 중복과 장황한 설명은 덜어내고, 오류나 이해의 빈틈은 필요한 만큼만 보충한다. 전체 재작성이나 추가 설명은 작성자가 요청했을 때만 한다.
- 수정안을 제시할 때는 원문에서 무엇이 왜 문제인지 짧게 밝힌다. 그림·식이 설명을 맡는 경우에는 글을 덜어내는 선택도 한다.

## 프로젝트 상세 페이지: 구현과 근거를 보여준다

아래 규칙은 프로젝트 상세 페이지를 작성하거나 수정할 때 적용한다. 블로그 글에 그대로 적용하지 않는다.

### 독자를 먼저 고려한다

- 독자가 저장소, 팀 내부 용어와 약어를 모른다고 가정한다.
- 기술명보다 먼저 그 기술이 프로젝트에서 무엇을 하는지 설명한다.
- 약어는 첫 등장에 쉬운 한국어 설명과 원어를 함께 쓴다. 이후에만 약어를 사용한다.
  - 좋은 예: `지상 위험요소 탐지 시스템(Ground IDS)`
  - 나쁜 예: `Ground IDS를 통합했습니다.`
- 풀네임이 불확실한 약어를 임의로 확장하지 않는다. 이때는 기능을 한국어로 설명한 뒤 프로젝트에서 쓰는 명칭을 괄호에 병기한다.

### 소제목을 템플릿처럼 만들지 않는다

- 모든 소제목을 `주제 — 설명`, `문제 — 해결`, `담당 범위 — 기술명` 형식으로 통일하지 않는다.
- 대시(`—`, `-`)는 두 개념의 대비나 단계 구분에 실제로 필요할 때만 사용한다.
- 소제목만 읽어도 해당 절에서 무엇을 보여주는지 알 수 있는 짧고 자연스러운 문구를 사용한다.
- 내부 명칭보다 독자가 찾는 기능이나 결과를 제목에 둔다.
  - 좋은 예: `CCTV 영상에서 관제 경보까지`, `ArUco Marker로 실제 위치 계산`, `2D 영상에서 버튼 위치 계산`
  - 피할 예: `서비스 흐름 — CCTV 영상에서 관제 경보까지`, `좌표변환 — ArUco Homography와 구역 판정`
- `My Role`, `담당 범위` 같은 고정 제목을 반복하지 않는다. 개인 기여는 실제로 맡은 기능을 제목으로 표현하고 본문 첫 문장에서 팀 규모와 책임을 밝힌다.

### 페이지 구조는 프로젝트 내용에 맞춘다

- 기본 흐름은 `Overview → 시스템 또는 사용 시나리오 → 핵심 구현 → 검증 결과 → 한계와 다음 개선`이다.
- 하드웨어가 이해에 중요하면 `Hardware`, 분산 구성이나 데이터 흐름이 중요하면 `Software/System`, 알고리즘 단계가 중요하면 기능별 절을 사용한다.
- 모든 프로젝트에 동일한 절 이름과 개수를 강제하지 않는다.
- 핵심 기능이 여러 단계라면 사용자가 경험하는 순서나 데이터가 흐르는 순서대로 배치한다.
- 팀 전체 기능을 나열하기보다 본인이 깊게 설명할 수 있는 기여를 중심으로 전개한다.

### 기술 설명은 근거와 함께 쓴다

- 각 핵심 절은 가능한 범위에서 `왜 필요했는지 → 어떻게 구현했는지 → 무엇으로 확인했는지`가 자연스럽게 이어지게 쓴다.
- 코드에 없는 기능, 측정하지 않은 성공률과 완료되지 않은 테스트를 구현 결과처럼 쓰지 않는다.
- 수치에는 평가 데이터, 조건과 의미를 붙인다. 수치해 허용 오차와 실제 장비 정확도처럼 서로 다른 지표를 명확히 구분한다.
- 실패와 한계는 숨기지 말고 소프트웨어 오차, 센서 부재, 기구 한계 등 원인 단위로 나눈다.
- 팀원의 기여와 본인의 기여를 구분하되, 본문을 인명 목록으로 만들지 않는다. 협업 관계가 구현 범위를 이해하는 데 필요할 때만 간결하게 밝힌다.

### 이미지는 장식이 아니라 증거로 사용한다

- 데모 영상, 시스템 구성도, 알고리즘 흐름도, Before/After, 학습 지표와 실제 장비 사진을 우선한다.
- 이미지는 관련 설명 바로 옆이나 직후에 배치한다.
- 같은 내용을 글, 코드, 흐름도로 반복하지 않는다. 이미지가 충분히 설명하면 본문은 해석과 핵심 판단만 덧붙인다.
- 캡션과 대체 텍스트는 화면에 보이는 대상과 그 이미지가 증명하는 내용을 구체적으로 적는다.
- 자료의 양보다 독자의 집중을 우선한다. 낮은 품질의 화면이나 핵심 기여와 무관한 GUI는 페이지를 길게 만들기 위해 추가하지 않는다.

### 문체

- 짧고 구체적인 문장을 사용한다.
- `고도화`, `최적화`, `혁신적`, `성공적으로` 같은 추상 표현은 실제 변경 사항이나 수치로 바꾼다.
- 기술 용어를 나열하기보다 입력, 처리, 출력과 프로젝트에서의 역할을 설명한다.
- 다른 우수 포트폴리오의 원칙은 참고하되 문장, 절 이름과 구성을 그대로 복제하지 않는다.
