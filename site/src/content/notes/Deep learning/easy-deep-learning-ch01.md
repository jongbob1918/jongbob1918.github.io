---
title: " 01. AI·ML·DL과 네 가지 학습 방식"
description: 인공지능, 머신러닝, 딥러닝의 관계와 지도·비지도·자기지도·강화학습의 차이를 정리합니다.
publishedAt: 2026-09-13
tags:
  - Deeplearning
draft: false
featured: false
---

## 1. AI, ML, DL의 차이

### 인공지능(Artificial Intelligence)

인공지능은 컴퓨터가 사람처럼 인식하고 판단하도록 만드는 기술을 아우르는 넓은 개념입니다. 사람이 규칙을 직접 정하는 방식부터, 데이터를 통해 학습하는 머신러닝과 딥러닝까지 포함합니다.

<img src="/images/notes/easy-deep-learning-ch01/ai-ml-dl-overview.png" alt="인공지능 안에 머신러닝과 딥러닝을 배치한 포함 관계와 알고리즘 예시" width="348" loading="lazy" />

### 머신러닝(Machine Learning)

**머신러닝**은 사람이 모든 판단 규칙을 직접 정하는 대신, 데이터에서 판단에 필요한 패턴을 학습하는 방법입니다.

규칙 기반 알고리즘으로 강아지와 고양이를 구분하려면 사람이 직접 구별 규칙을 만들어야 합니다. 하지만 같은 동물도 생김새와 자세가 달라 모든 경우에 맞는 규칙을 작성하기는 어렵습니다.

<img src="/images/notes/easy-deep-learning-ch01/rule-based-classification.png" alt="강아지와 고양이 사진 옆에 사람이 작성한 구별 규칙을 나열한 예시" width="795" loading="lazy" />

머신러닝에서는 강아지와 고양이 사진을 정답과 함께 제공해, 모델이 둘을 구분하는 패턴을 학습하도록 합니다.

<img src="/images/notes/easy-deep-learning-ch01/data-based-learning.png" alt="강아지와 고양이 사진을 컴퓨터에 입력하는 데이터 기반 학습 흐름" width="797" loading="lazy" />

다양한 상황의 사진을 구분하려면 학습 데이터도 그 상황을 충분히 담고 있어야 합니다. 예를 들어, 누워 있거나 뒤돌아 있는 모습, 흐릿하거나 노이즈가 있는 사진을 포함하면 다양한 입력에 대응하는 데 도움이 됩니다. 다만 데이터가 많고 다양하다는 이유만으로 높은 성능이 보장되지는 않습니다.

이렇게 데이터로 모델을 조정하는 과정을 **훈련**이라고 합니다. 훈련 후에는 **학습에 사용하지 않은 사진**으로 강아지와 고양이를 얼마나 잘 구분하는지 평가합니다.

### 딥러닝(Deep Learning)

**딥러닝**은 머신러닝의 한 종류입니다. 여러 층으로 이루어진 **심층 신경망(Deep Neural Network)**을 이용해 데이터에서 패턴을 학습합니다.

<img src="/images/notes/easy-deep-learning-ch01/deep-neural-network.png" alt="입력층에서 여러 은닉층을 거쳐 출력층으로 연결되는 인공신경망 구조" width="516" loading="lazy" />

사진 출처: https://velog.io/@ceunseo313/%EB%94%A5%EB%9F%AC%EB%8B%9D-%EA%B8%B0%EB%B3%B8-%EA%B0%9C%EB%85%90-1%ED%83%84

**인공신경망(Artificial Neural Network)**은 뇌의 신경세포와 그 연결 방식에서 영감을 얻은 계산 모델입니다.


<img src="/images/notes/easy-deep-learning-ch01/neuron-connections.png" alt="신호를 전달하는 신경세포들의 연결과 인공신경망의 생물학적 영감" width="640" loading="lazy" />

사진 출처: https://blog.naver.com/nlrose/223836604859

**특징 추출**은 데이터에서 판단에 필요한 정보를 뽑는 과정입니다. 전통적인 이미지 분류에서는 사람이 특징을 추출하는 방법을 정하고, 모델은 그 특징으로 이미지를 구분하는 방법을 학습하는 경우가 많았습니다. **딥러닝은 특징을 추출하는 방법부터 이미지를 구분하는 방법까지 함께 학습할 수 있습니다.**

예를 들어, 손글씨 숫자를 읽는 두 가지 방식을 비교해 보겠습니다. 두 방식 모두 숫자 이미지와 정답을 이용해 학습합니다.

- **사람이 특징을 설계하는 방식:** 이미지를 작은 구역으로 나누고, 각 구역에 가로·세로·대각선 방향의 획이 얼마나 나타나는지 계산합니다. 모델은 이렇게 얻은 숫자들을 이용해 손글씨가 어떤 숫자인지 구분하는 방법을 학습합니다.
- **딥러닝 방식:** 이미지의 픽셀 값을 신경망에 입력합니다. 신경망은 정답을 맞히도록 학습하면서, 구분에 유용한 패턴을 뽑는 방법과 그 패턴으로 숫자를 판단하는 방법을 함께 조정합니다.

첫 방식에서는 사람이 정한 특징 추출 방법이 고정되어 있고, 두 번째 방식에서는 특징 추출 방법도 학습하면서 바뀝니다.

참고: [MathWorks · Deep Learning](https://www.mathworks.com/discovery/deep-learning.html), [Deep Learning · 1장](https://www.deeplearningbook.org/contents/intro.html)

## 2. 머신러닝의 학습 방식

이 글에서는 지도학습, 비지도학습, 자기지도학습, 강화학습을 살펴봅니다.

### 2.1 지도학습

강아지 사진을 모델에 넣었을 때, 예측이 맞았는지 판단할 기준이 필요하다. **지도학습**은 입력에 정답을 붙이고, 예측과 정답의 차이를 줄이도록 학습하는 방식이다. 테스트에서는 정답을 입력하지 않고 평가에만 사용한다.

입력 데이터에 정답을 붙이는 작업을 **데이터 레이블링**(Data Labeling)이라고 한다.

#### 분류와 위치 추정

사진에 무엇이 있는지만 알면 되는지, 어디에 몇 개 있는지도 알아야 하는지에 따라 작업이 달라진다.

- **분류:** 이미지가 무엇을 나타내는지 클래스를 예측한다. 예: 강아지, 고양이.
- **객체 위치 추정:** 대상 객체의 위치를 예측한다. 보통 경계 상자로 표시한다.
- **객체 탐지:** 한 이미지에 있는 여러 객체의 종류와 위치를 함께 예측한다.

#### 분할

경계 상자만으로는 물체의 정확한 모양을 표현하기 어렵다. **분할**은 픽셀 단위로 영역을 구분한다.

- **의미 분할:** 모든 픽셀에 대해 어떤 클래스에 속하는지 판단한다.
- **인스턴스 분할:** 같은 클래스라도 개별 객체를 구분한다. 사람 두 명이 있으면 각각 다른 영역으로 표시한다.

구현 예시: 객체 탐지 → 각 객체 영역의 픽셀 분할

#### 자세 추정과 얼굴 랜드마크 탐지

- **자세 추정(Pose Estimation):** 어깨·팔꿈치·무릎 등 주요 신체 부위의 좌표를 예측한다.
- **얼굴 랜드마크 탐지:** 눈·코·입 등 얼굴의 주요 특징점 좌표를 예측한다.

지도학습에는 정확한 레이블이 필요하다. 사람이 위치와 영역을 직접 표시하는 작업은 시간과 비용이 많이 든다.

### 2.2 자기지도학습

데이터가 많아도 사람이 정답을 붙이는 데는 시간과 비용이 든다. **자기지도학습**은 데이터 자체에서 학습 목표를 만들어 레이블링 부담을 줄이는 방식이다.

#### 사전학습과 미세조정

실제 작업의 레이블이 적으면 충분한 학습이 어려울 수 있다. 대표적인 활용 흐름은 **레이블 없는 데이터로 먼저 특징을 학습하고, 실제 작업의 레이블로 모델을 조정하는 것**이다.

| 단계 | 학습 내용 | 사용하는 데이터 |
| --- | --- | --- |
| 사전학습(Pretraining) | 보조 과제인 프리텍스트 작업(Pretext Task)을 풀며 특징 학습 | 사람이 붙인 레이블이 없는 데이터 |
| 미세조정(Fine-tuning) | 실제로 풀려는 다운스트림 작업(Downstream Task)에 맞게 모델 조정 | 해당 작업의 레이블이 있는 데이터 |

#### Context Prediction · 위치 관계 예측

이미지에 정답 레이블이 없어도 어디에서 잘라낸 조각인지는 알 수 있다. 이 위치 정보를 학습 목표로 사용하는 방법이 **Context Prediction**이다. 작은 이미지 영역인 패치 사이의 상대 위치를 예측한다.

1. 이미지에서 기준 패치를 무작위로 선택한다.
2. 기준 패치 주변에서 같은 크기의 패치를 선택한다.
3. 두 패치를 입력하고, 주변 패치가 기준 패치의 어느 방향에 있었는지 예측한다.

<figure>
  <img src="/images/notes/easy-deep-learning-ch01/context-prediction.jpg" alt="고양이 코의 기준 패치와 귀 패치로 상대 위치 3번을 예측하는 Context Prediction 원 저자 그림" width="1024" loading="lazy" />
  <figcaption>출처: Doersch · Gupta · Efros, <a href="https://graphics.cs.cmu.edu/projects/deepContext/">ICCV 2015 프로젝트 페이지</a>.</figcaption>
</figure>

패치의 위치는 추출 과정에서 알 수 있으므로 별도 레이블링 없이 여러 학습 쌍을 만들 수 있다. 이미지의 픽셀과 물체의 부분이 일정한 구조를 가진다는 점을 활용한다. [원 논문](https://arxiv.org/abs/1505.05192)

#### Contrastive Learning · 대조학습

사진을 자르거나 밝기를 바꿔도 같은 대상의 특징을 유지할 필요가 있다. **대조학습**에서는 같은 원본의 두 변형을 한 쌍으로 만들어, 공통된 특징을 학습하게 한다. 아래는 SimCLR 방식의 예다.

1. 하나의 이미지에 서로 다른 변형을 적용한다. 예: 일부 자르기, 밝기·색상 조절.
2. 변형된 이미지를 모델에 넣어 각각 숫자 벡터로 표현한다.
3. 같은 원본의 벡터는 가까워지고, 다른 원본의 벡터는 멀어지도록 모델을 조정한다.

<figure>
  <img src="/images/notes/easy-deep-learning-ch01/simclr.png" alt="강아지와 의자 사진을 각각 두 번 변형해 같은 원본의 표현은 당기고 다른 원본은 밀어내는 SimCLR 그림" width="640" loading="lazy" />
  <figcaption>출처: Chen et al., <a href="https://simclr.github.io/">SimCLR 공식 프로젝트 페이지</a>.</figcaption>
</figure>

두 변형의 표현이 가까워지도록 모델을 조정하면서, 변형 후에도 공통으로 남는 특징을 학습한다. [SimCLR 원 논문](https://arxiv.org/abs/2002.05709)

#### 자연어 처리

- **GPT:** 앞선 토큰을 보고 다음 토큰을 예측한다(Next Token Prediction).
- **BERT:** 문장의 일부를 가리고 해당 토큰을 예측한다(Masked Token Prediction). 원래 BERT는 두 문장 구간이 연속된 것인지 예측하는 작업(Next Sentence Prediction)도 사용했다.

두 방식 모두 원문으로부터 학습 목표를 만든다. [BERT 공식 설명](https://github.com/google-research/bert#what-is-bert)

### 2.3 비지도학습

**레이블이 없는 데이터에서 패턴이나 구조를 찾는 방식.**

대표 예시: 군집화, 차원 축소

자기지도학습은 데이터에서 예측할 문제와 정답을 만든다. 여기서 다루는 비지도학습은 데이터의 유사성이나 분포를 이용해 구조를 찾는다.

#### 군집화

정답 분류가 없는 데이터에서 비슷한 사례끼리 묶어 보고 싶을 때 사용한다. **군집화**는 데이터의 특징을 비교해 그룹을 찾는다.

- **K-means:** 군집 수 K를 정하고, 가까운 중심에 데이터를 배정한 뒤 중심을 갱신하는 과정을 반복한다.
- **DBSCAN:** 데이터가 밀집한 영역을 그룹으로 묶는 밀도 기반 방법이다.

#### 차원 축소

데이터의 변수가 많으면 계산량이 늘고, 분포를 눈으로 확인하기도 어렵다. **차원 축소**는 중요한 특징을 가능한 한 유지하면서 변수 수를 줄이는 방법이다.

활용 예시: 고차원 데이터 시각화, 계산량 감소

- **PCA · 주성분 분석:** 데이터의 분산을 크게 설명하는 방향으로 투영해 차원을 줄인다.
- **SVD · 특잇값 분해:** 행렬 분해 도구다. 일부 성분을 남기는 근사나 PCA 계산에 활용할 수 있다.

[군집화·차원 축소 참고 문서](https://scikit-learn.org/stable/unsupervised_learning.html)

### 2.4 강화학습

**강화학습(Reinforcement Learning)은 행동을 해 보고, 그 결과로 받은 점수를 이용해 다음 선택을 개선하는 학습 방식입니다.** 한 번의 선택뿐 아니라, 여러 선택 뒤에 얻는 결과까지 고려합니다.

오목을 배우는 AI를 생각해 보겠습니다. 오목은 두 선수가 번갈아 돌을 놓고, 자기 돌 다섯 개를 한 줄로 먼저 연결하면 이기는 게임입니다. 여기서는 AI가 검은 돌을 두며, 연습 상대의 행동 방식은 고정되어 있다고 가정합니다.

#### 정답 위치 대신 승패에 점수를 줍니다

지도학습으로 가르치려면 각 상황에서 둘 위치를 정답으로 준비할 수 있습니다. 하지만 가능한 돌의 배치마다 좋은 수를 알려주기는 어렵습니다. 반면 **누가 이겼는지는 게임 규칙으로 판정할 수 있습니다.**

그래서 이 예시에서는 사람이 다음 점수 규칙을 정합니다. 이 점수를 **보상**(Reward)이라고 합니다.

| AI가 얻은 결과 | 보상 |
| --- | --- |
| 승리 | +1 |
| 패배 | −1 |
| 아직 진행 중이거나 무승부 | 0 |

AI는 게임에서 더 높은 보상을 얻도록 학습합니다. 이 점수 규칙에서는 패배보다 무승부가, 무승부보다 승리가 유리합니다. **사람은 목표를 점수로 정하고, AI는 그 목표에 도달할 행동을 경험에서 배웁니다.** 강화학습에서 보상을 언제, 얼마나 줄지는 문제에 따라 달라집니다.

오목에서 학습은 다음 순서로 반복됩니다.

1. 현재 돌의 배치를 봅니다.
2. 빈칸 하나를 골라 돌을 놓습니다.
3. 상대의 응수와 승패를 확인합니다.
4. 받은 보상과 바뀐 판을 이용해, 방금 선택에 대한 평가를 고칩니다.

수를 선택하는 AI를 **에이전트**(Agent), 게임 규칙과 상대를 포함한 게임을 **환경**(Environment)이라고 합니다. 현재 판과 차례는 **상태**(State), 돌을 놓는 선택은 **행동**(Action)입니다. 한 판 전체는 **에피소드**(Episode)라고 합니다.

#### 지금 받은 0점과 앞으로의 가능성은 다릅니다

검은 돌 네 개가 이어졌지만 아직 다섯 개는 아닌 상황입니다. 현재 보상은 0입니다. 그래도 다음에 다섯 개를 만들 수 있다면 유리한 상황입니다.

<figure>
  <img src="/images/notes/easy-deep-learning-ch01/gomoku-open-four.png" alt="검은 돌 네 개가 가로로 이어지고 양 끝의 빈칸에 빨간 점을 표시한 오목판" width="420" loading="lazy" />
  <figcaption>아직 승리 보상은 없지만, 빨간 점에 검은 돌을 놓으면 다섯 개가 이어집니다. 출처: <a href="https://asobigame.com/game/gomoku">Asobi Game · 오목 안내</a>.</figcaption>
</figure>

당장 받은 보상만 보면, 승리 직전의 수와 도움이 되지 않는 수가 모두 0점일 수 있습니다. 따라서 **그 수를 둔 뒤 앞으로 받을 보상도 예상해야 합니다.**

이처럼 현재 판에서 어떤 수를 선택했을 때, 이후 행동까지 이어 가며 얻을 보상을 예상한 값을 **행동 가치**(Q값)라고 합니다. 보상은 게임에서 받은 실제 점수이고, Q값은 AI가 경험으로 고쳐 나가는 예상치입니다.

같은 판에서 A칸의 Q값을 0.6, B칸을 0.2로 예상한다면 현재 평가는 A칸이 더 좋다는 뜻입니다. 이 숫자는 설명용 가정이며, **0.6이 승률 60%라는 뜻은 아닙니다.** 판의 배치가 달라지면 같은 칸의 평가도 달라집니다.

#### 마지막 승리를 앞선 선택의 평가에 반영합니다

AI는 처음부터 좋은 수를 알지 못합니다. 예를 들어 모든 Q값을 0으로 시작하고, 여러 수를 시도하면서 평가를 바꿀 수 있습니다.

경험을 이용해 Q값을 고치는 방법 중 하나가 **큐러닝**(Q-learning)입니다. 핵심은 **이번에 받은 보상과, 다음 판에서 기대할 수 있는 보상을 함께 보고 현재 선택을 다시 평가하는 것**입니다.

1. **다섯 번째 돌을 놓아 이겼습니다.** +1을 받았으므로, 그 판에서 마지막 수를 선택한 평가를 +1 쪽으로 올립니다.
2. **그 직전 선택을 다시 학습합니다.** 당시 보상은 0이었지만, 그 선택 뒤에는 높은 평가를 받는 수를 둘 기회가 생겼습니다. 이 다음 상황의 평가를 앞선 선택에도 반영합니다.
3. **이 과정을 반복합니다.** 마지막 승리에서 얻은 정보가 더 앞선 선택들의 평가에도 영향을 줍니다.

한 번 이겼다고 그 판의 모든 수가 좋은 수로 확정되지는 않습니다. 상대의 실수로 이겼을 수도 있기 때문입니다. 여러 승패 경험을 반영하며 평가를 수정합니다. 큐러닝은 강화학습의 한 방법이며, 모든 강화학습이 Q값을 사용하는 것은 아닙니다.

#### 처음의 예상만 믿지 않고 다른 수도 시도합니다

아직 B칸을 충분히 시도하지 않았다면, 낮은 Q값은 나쁜 수라는 증거가 아닐 수 있습니다. A칸만 계속 고르면 B칸의 가능성을 확인하지 못합니다.

새로운 선택을 시험하는 것을 **탐색**(Exploration), 지금까지 배운 평가를 이용하는 것을 **활용**(Exploitation)이라고 합니다. 항상 무작위로 두면 배운 것을 쓰지 못하고, 항상 최고 점수만 고르면 다른 가능성을 놓칩니다.

두 행동을 섞는 한 방법이 **엡실론 그리디**(ε-greedy)입니다. 예를 들어 ε를 0.1로 정하면, 매번 수를 고를 때 다음 규칙을 적용합니다.

- **10% 확률로 탐색:** 둘 수 있는 빈칸 중 무작위로 고릅니다.
- **90% 확률로 활용:** 현재 Q값이 가장 높은 수를 고릅니다.

이는 열 번마다 반드시 한 번 탐색한다는 뜻이 아닙니다. 매 선택에 적용하는 확률이며, 탐색하다가 최고 점수의 수를 고를 수도 있습니다.

#### Q값을 실제로 고치는 계산

아래 계산은 앞서 설명한 ‘다음 상황의 평가를 앞선 선택에 반영한다’를 숫자로 확인하는 예시입니다.

<details>
<summary>미래 보상을 얼마나 반영할까? 할인율 γ</summary>

미래 보상을 현재 평가에 반영하는 비율을 **할인율 γ**(감마)라고 합니다. γ가 0.9라면 보상이 한 단계 늦어질 때마다 0.9를 곱합니다.

여기서 한 단계는 내가 수를 두고, 게임이 끝나지 않았다면 상대가 응수해 다시 내 차례가 되기까지입니다. 도중에 승패가 나면 그 단계에서 종료합니다.

지금부터 받을 보상이 `0 → 0 → +1`이라면, 현재 시점에서 계산한 합은 다음과 같습니다.

```text
0 + 0.9 × 0 + 0.9² × 1 = 0.81
```

**실제로 받은 승리 보상은 여전히 +1**입니다. 0.81은 늦게 받은 보상을 현재 평가에 반영하기 위해 계산한 값입니다. Q값은 이런 가능한 결과들을 평균적으로 얼마나 기대하는지 나타냅니다.

γ를 작게 두면 가까운 보상을 더 중시하고, 1에 가깝게 두면 먼 미래의 보상도 더 반영합니다. 오목처럼 한 판이 끝나는 문제에서는 γ를 1로 두어, 승리까지 걸린 시간에 따라 보상을 줄이지 않을 수도 있습니다.

</details>

<details>
<summary>기존 Q값은 얼마나 바꿀까? 큐러닝 계산 예시</summary>

아직 게임이 끝나지 않았다면, 다음 내 차례에서 둘 수 있는 수 중 가장 높은 Q값으로 미래 보상을 예상합니다. 현재 판에서 선택한 수의 평가를 다음 목표에 가깝게 조정합니다.

```text
목표값 = 이번 보상 + 할인율 × 다음 판의 최대 Q값
새 Q값 = 기존 Q값 + 학습률 × (목표값 − 기존 Q값)
```

**학습률 α**(알파)는 새 경험을 반영해 기존 평가를 얼마나 바꿀지 정하는 값입니다. 0.5라면 목표값까지 차이의 절반만큼 이동합니다.

기존 Q값이 0.2, 이번 보상이 0, 다음 판의 최대 Q값이 1이라고 가정하겠습니다. 할인율은 0.9, 학습률은 0.5로 정합니다.

```text
목표값 = 0 + 0.9 × 1 = 0.9
새 Q값 = 0.2 + 0.5 × (0.9 − 0.2) = 0.55
```

당장 보상을 받지 않았어도, **다음에 좋은 선택을 할 수 있는 상황으로 이어졌기 때문에** 현재 평가가 0.2에서 0.55로 올라갔습니다. 다음 판의 평가도 추정치이므로, 새 경험을 통해 계속 수정합니다.

승패나 무승부로 게임이 끝났다면 다음 행동은 없습니다. 이때 목표값은 마지막 보상만 사용합니다. 갱신은 게임 도중에도, 기록한 경험을 다시 학습할 때도 할 수 있습니다.

</details>

참고: [Sutton·Barto · 강화학습 교재](https://www.incompleteideas.net/book/bookdraft2018mar21.pdf), [Gymnasium · 큐러닝 예제](https://gymnasium.farama.org/tutorials/training_agents/frozenlake_q_learning/)

## 참고 자료

- 혁펜하임, 『이지 딥러닝』, 챕터 1.
- Doersch et al., [Context Prediction](https://arxiv.org/abs/1505.05192), 2015.
- Chen et al., [SimCLR](https://arxiv.org/abs/2002.05709), 2020.
- [BERT 공식 설명](https://github.com/google-research/bert#what-is-bert).
- [scikit-learn 비지도학습 문서](https://scikit-learn.org/stable/unsupervised_learning.html).
- [Spinning Up 강화학습 기본 개념](https://spinningup.openai.com/en/latest/spinningup/rl_intro.html).
